# Generated by copier - do not edit manually
#
# Estaba vacío (solo esta cabecera), así que `agents/config.py` caía a sus
# defaults y `project_slug` quedaba en "" — eso rompía en silencio a los agentes
# que derivan rutas del paquete: `review review_package`, `mlflow` (nombre del
# experimento) y `cicd generate_workflow`. Rellenado a mano con los valores
# reales del proyecto.
project_name: ClimaSafeAI
project_slug: climasafeai
project_author_name: Alejandro Cancelas Chapela
project_description: Prediccion de riesgo climatico personalizado (calor/frio/uv) con ML
project_version: 0.0.1
python_version: '3.13'

ml_type: supervisado
task_type: clasificacion
model_type: todos
nn_model: MLP

use_mlflow: true
use_monitoring: true
use_optuna: true
use_duckdb: false
use_api: false
use_docker: false
use_shap: true
use_xgboost: true
use_lightgbm: true
use_catboost: false
